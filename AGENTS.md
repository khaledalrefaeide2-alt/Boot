# الإعلام الرقمي — تعليمات المشروع للوكلاء

> هذا الملف يُقرأ تلقائياً في بيئات التطوير الوكيلية (Google Antigravity،
> Cursor، Claude Code، Codex وغيرها). اقرأه قبل أي تعديل.
> **بالعربية لأن المشروع عربي**: كل النصوص والتعليقات وأسماء الاختبارات
> بالعربية، والواجهة RTL. اكتب بالعربية أيضاً.

## ما هو المشروع

أداة فرز محتوى (Trust & Safety) تستخرج منشورات صفحات فيسبوك وتغريدات X
عبر واجهة Apify، ثم تصنّفها محلياً على إطار قانوني وأخلاقي سوري.
المستعمل مسؤول محتوى يمرّ على مئات المنشورات في الجلسة.

## القيد الحاكم — اقرأه قبل كل شيء

**الموقع يُفتح من القرص بالنقر المزدوج (`file://`)، وقد يعمل بلا إنترنت.**

هذا ليس تفضيلاً بل شرط الاستعمال، ويترتّب عليه:

| ممنوع | لماذا |
|---|---|
| أي `<script src="https://…">` أو `<link href="https://…">` | يعطب الموقع بلا إنترنت |
| خطوط Google أو أي CDN | نفس السبب |
| وحدات ES (`type="module"`) | تفشل بسياسة المصدر نفسه من `file://` |
| خطوة بناء (Vite/Webpack/Tailwind) | لا يوجد `npm run build`؛ الملفات تُفتح كما هي |
| أُطر عمل (React/Next/Vue) | تفترض خادماً أو تصريفاً |

الشبكة تُستعمل **فقط** لواجهة Apify و(اختيارياً) قاعدة البيانات المحلية.
اختبار في `tests/shell.test.js` يفحص كل وسم تحميل في الصفحات الخمس ويفشل
عند أوّل مرجع خارجي.

## بنية المستودع

| المجلد | ما هو |
|---|---|
| `digital-media-v6/` | **النسخة الحيّة** — نظام «السجلّ»: ورق فاتح، حبر داكن، خطوط شعرية |
| `facebook-extractor/` | الإصدار الخامس — هوية داكنة بالزجاج والأبعاد الثلاثية. محفوظ للمقارنة، لا يُطوَّر |
| `digital-media-android/` | غلاف Capacitor. `www/` فيه **نسخة لا مصدر** — تُحدَّث بـ `npm run sync` |
| `docs/architecture.html` | المواصفة المعمارية |
| `design-system/` | مخرجات مهارة ui-ux-pro-max (مرجع، غير متعقَّب في git) |

**كل تطوير في `digital-media-v6/`.** ثم `cd digital-media-android && npm run sync`.

## الوحدات — ولماذا لا تُشقّ نسختين

```
icons.js      مصدر الأيقونات الوحيد + ورقة الرموز (<symbol>/<use>)
analyzer.js   محرّك التصنيف — لا يُمسّ إلا بطلب صريح
common.js     المشترك: التخزين، الشبكة، السمة، القائمة، FBXSave، debounce
auth.js       جلسة المدير — بوّابة سلوكية لا خادم حقيقي (راجع القسم أدناه)
card.js       بناء بطاقة المنشور وصفّ الجدول
media.js      معرض الصور والعارض المكبّر
pages.js      دليل الصفحات
ai.js / report.js / xlsx-*.js   المراجعة الذكية والتقارير والتصدير
theme.css     نظام التصميم كلّه — الهوية تتبدّل من هنا وحده
```

الوحدات مشتركة بين النسختين. **لا تنسخ وحدة لتعدّلها**؛ أضف نقطة تبديل
(seam) بدل ذلك — كما في `W.FBXSave` الذي يستبدله غلاف أندرويد بكتابة
فعلية على القرص.

## الفصل بين لوحة التحكّم ولوحة عرض النتائج

خمس صفحات إدارية (`index.html`، `twitter.html`، `dashboard.html`،
`twitter-dashboard.html`، `settings.html`) تتطلّب **جلسة مدير**؛
`results.html` مفتوحة للجميع بلا تسجيل دخول — استعراض وبحث وتصنيف فقط،
بلا أي إجراء. `login.html` بوّابة الدخول ومسار إنشاء الحساب الأوّل.

**هذه بوّابة سلوكية لا حماية حقيقية** — والسبب هو القيد الحاكم نفسه أعلاه:
لا خادم يتحقّق من الهوية على طرفه، فمن يملك المجلد يستطيع فتح أي صفحة
إدارية مباشرةً متجاوزاً البوّابة. التفصيل الكامل والمبرَّر في تعليق أعلى
`auth.js`. لا تُقدَّم هذه الآلية للمستخدم على أنها أمان حقيقي، ولا تُبنى
عليها قرارات حساسة (مثل كشف بيانات لا يجوز كشفها لولا البوّابة).

القواعد عند التعديل في هذه المنطقة:
- الصفحات الإدارية الخمس تحمل نفس حارس ما قبل الرسم: فحص
  `fbx_admin_session` كأوّل سطر في سكربت `<head>`، قبل حتى قراءة المظهر.
  أي صفحة إدارية جديدة يجب أن تحمله بالترتيب نفسه.
- `results.html` لا تُحمِّل `pages.js`/`ai.js`/`report.js`/`xlsx-import.js`/
  `xlsx-export.js` إطلاقاً — الحدّ الفاصل بنيوي (الوحدة غير موجودة في
  الصفحة) لا سلوكي (زرّ مخفيّ يمكن كشفه)، وهذا مقصود ويجب أن يبقى كذلك.
- أي قسم في `results.html` يُبنى بالكامل من JS بلا بيانات معروفة مسبقاً
  (كملخّص التصنيف والفلاتر) يجب أن يبقى داخل `<section id="results"
  hidden>` — الكشف يقع بعد الرسم المتزامن الأوّل. إزالة `hidden` أو نقل
  الرسم إلى دالّة غير متزامنة يُعيد إزاحة تخطيط قِيست عند 0.0562.
- `results.html` تقرأ بيانات هذا المتصفح محلياً (`fbx_monitor_feed`
  و`fbx_x_tweet_monitor_feed`) دائماً، وتدمج معها قاعدة البيانات المحلية
  إن كانت مضبوطة ومتصلة (`FBXCommon.db.url` — **ليس** `db.url` وحده؛ هذا
  الاسم غير مصدَّر كمتغيّر عام عمداً، خلافاً لبقية دوال common.js).
  الشارة `#sourceNoteText` يجب أن تبقى تُفصح عن أيّ النطاقين فعّال.
- `common.js` يحمل `DEFAULT_DB_URL` — عنوان احتياطي فارغ افتراضياً يُستعمل
  فقط حين لا يضبط هذا المتصفح عنواناً خاصّاً به من صفحة الإعدادات. يُملأ
  بعد نشر `local-server/server.js` على استضافة عامة دائمة التشغيل، ليرى
  أيّ زائر لرابط `results.html` العام (مثل نسخة GitHub Pages أدناه)
  البيانات الحيّة بلا أيّ إعداد من طرفه. إعداد الجهاز الصريح (`localStorage`)
  يبقى له الأولوية دائماً — راجع ترتيب `||` في تعريف `db.url`.

### نشر رابط عام لعرض النتائج فقط

`.github/workflows/deploy-results-pages.yml` ينشر نسخة قراءة فقط من
`results.html` وملحقاته الضرورية (`theme.css`، `icons.js`، `analyzer.js`،
`card.js`، `common.js`، `media.js`) على GitHub Pages — **بلا** أيّ صفحة
إدارية أو `login.html` أو `auth.js`، فلا واجهة استخراج أو دخول مدير تصل
الإنترنت أصلاً مهما حدث. رابط «دخول المدير» في هذه النسخة يبقى في
الملفّ لكنه يشير إلى صفحة غير منشورة (يفشل عمداً، وهذا مقبول ومقصود).
تفعيل النشر يتطلّب خطوة يدوية لمرّة واحدة: *Settings → Pages → Build and
deployment → Source: GitHub Actions* — الملفّ نفسه لا يُفعّلها.
البيانات الحيّة على هذا الرابط تتطلّب أيضاً نشر `local-server/server.js`
على خدمة استضافة عامة دائمة التشغيل (مع تخزين دائم لملفّ SQLite لا
ينمحي عند كل إعادة نشر)، ثمّ تعبئة عنوانها في `DEFAULT_DB_URL` أعلاه.

## قواعد لا تُخالَف — كلٌّ منها وُلد من عطل مقيس

### 1. اللون كلّه للحكم
لا شيء في الواجهة ملوَّن إلا ما يحمل دلالة تصنيف. الإطار حبر ورماد،
والعلامة نيليّ واحد. الأخضر والكهرماني والأحمر **ملك محرّك التصنيف**.
اختبار يفشل إن تسرّب لون دلالة إلى قواعد القشرة.

وكل حكم يصل بثلاث طرق مستقلّة (قضيب + شارة نصّية + مقياس شُرَط) لأن
الإرشاد يمنع نقل المعلومة باللون وحده.

### 2. لا لون مثبَّت في سمة `style` السطرية
السطرية تغلب ورقة الأنماط فتبقى كما هي في المظهرين. زرّ «رصد فوري» كان
يحمل نصّاً أبيض من يوم كان الشريط كحلياً، فاختفى تماماً على الورق.

### 3. الحالة تُقرأ قبل أوّل رسمة
المظهر، ووجود المفتاح، وطيّ القائمة — كلّها تُقرأ في سكربت داخل
`<head>` قبل ورقة الأنماط. قراءتها بعد التحميل تُنتج إزاحة تخطيط
(قِيست 0.0219 و0.1028 قبل الإصلاح؛ الهدف **CLS = 0**).

### 4. الأيقونات إشارة لا تضمين
`FBXIcons.svg()` يُخرج `<use href="#fbx-i-…">`. تضمين المسارات في كل
موضع كلّف **800ms من أصل 1313** في تحليل `innerHTML` عند 1500 بطاقة.
ومقاس الصندوق معرَّف في `theme.css` لا في `icons.js` — لأن الأوّل يحجب
الرسم فيحجز المكان قبل الحقن.

### 5. ما لا يُرى لا يُبنى
لوحة التحليل تُبنى عند أوّل فتح لا مع البطاقة (22500 عنصر مهدور).
والبحث مؤخَّر 160ms (كتابة ست أحرف على 1500 منشور: **38 ثانية ← 1.06**).

### 6. التباين يُقاس لا يُفترض
الحدّ 4.5:1، وأرضية مقاس الخطّ **12px**. القياس من بكسلات مرسومة بعد
تركيب الشفافية، لا من الرموز — أرضية مغسولة أو شبه شفافة تُغيّر النتيجة.

### 7. الوصول شرط لا تحسين
حلقة تركيز 3px، وهدف لمس 44×44 (بمنطقة شفافة لا بتضخيم الشكل)، ورابط
تخطٍّ، ومعالم ARIA معنونة، وعنوان `h1` واحد هو عنوان الصفحة.
والتسميات المطويّة تُخفى **بصرياً** لا بـ `display:none` — الأخير يمحوها
من شجرة الوصول فتصير روابط بلا أسماء.

### 8. العربية خطّ متّصل
لا خطّ أحادي العرض ولا تباعد حرفي على النصّ العربي — يفكّان وصل الحروف.
الأحادي (`--mono`) للأرقام والمعرّفات والأوقات اللاتينية وحدها، مع
`tabular-nums` لأن الأعمدة تُقارَن رأسياً في أداة فرز.

## قبل أن تسلّم أي تعديل

```bash
cd digital-media-v6
node --test "tests/*.test.js"        # 181 اختباراً — يجب أن تمرّ كلّها
```

الاختبارات ليست تغطيةً شكلية: كلّ واحد منها وُلد من عطل حقيقي وجُرّب
بإرجاع العطل فسقط فعلاً. `tests/shell.test.js` خاصّةً يحرس القواعد
الثمانية أعلاه.

وللتحقّق البصري، افتح `digital-media-v6/index.html` في المتصفّح مباشرةً
— لا تشغّل خادماً إلا للاختبار الآلي، فالمستخدم يفتحه من القرص.

## أعطال معروفة وقعت سابقاً — لا تُعِدها

| العطل | السبب |
|---|---|
| توقّف الرصد التلقائي بصمت | حذف `p.key` من `normalizePost` جعل مجموعة إزالة التكرار `{undefined}` |
| «Failed to fetch» من `file://` | `POST` بترويسة JSON يستدعي فحصاً مسبقاً، و`file://` يرسل `Origin: null`. الحلّ في `apiPost`: `text/plain` أولاً |
| `:hover` توقّف بلا خطأ | `animation-fill-mode: both` يثبّت التحوّل النهائي فيغلب `:hover`. الصواب `backwards` |
| زرّ «دليل الصفحات» لا يفتح | `ic()` مستدعاة بلا تعريف في `pages.js` — الخطأ داخل معالج نقرة فلا يوقف الصفحة |
| عنصر يخرج عن حاويته 7px | `width: 100%` مع `margin` جانبي. الحشو على الحاوية لا هوامش على أبنائها |
| `db is not defined` في results.html | `db` وحده (خلافاً لبقية common.js) غير مصدَّر كمتغيّر عام عمداً — استعمل `FBXCommon.db.url` |
| إزاحة تخطيط 0.0562 في results.html | قسم يُبنى بالكامل من JS بلا `hidden` يُرسم فارغاً في إطار أول أثناء تحميل السكربتات المتتابعة، ثم يقفز عند اكتمال الرسم |

## المفاتيح والخصوصية

مفتاح Apify ومفتاح المراجعة الذكية يعيشان في `localStorage` على جهاز
المستخدم وحده. **لا تُرسلهما إلى أي وجهة** غير نقطتَي النهاية اللتين
ضبطهما المستخدم، ولا تكتبهما في مستودع ولا سجلّ.
