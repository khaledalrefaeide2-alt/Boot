# Peace Emergency Digital Information Platform — runtime dependencies
fastapi>=0.115
uvicorn[standard]>=0.30
jinja2>=3.1
sqlalchemy>=2.0
pydantic>=2.7
pydantic-settings>=2.3
python-multipart>=0.0.9
itsdangerous>=2.2
bcrypt>=4.1

# AI / RAG. Optional at runtime: the platform falls back to offline
# deterministic providers when no API key is configured (see app/ai/providers).
anthropic>=0.40

# PostgreSQL driver (production). SQLite is used for local/demo and needs no driver.
psycopg[binary]>=3.1 ; python_version >= "3.9"

# Testing
pytest>=8.0
httpx>=0.27
