/**
 * مفردات الحركة — منحنيات ومدد ثابتة تُستدعى بالاسم لا تُكتب بالأرقام.
 *
 * السبب هو السبب نفسه وراء مصفوفة الارتفاع: قيمة مكتوبة داخل مكوّن تصير
 * قيمة أخرى في المكوّن التالي، فتتفكّك الواجهة إلى حركات لا يجمعها شيء.
 * وأثر ذلك لا يُرى في لقطة شاشة بل يُحسّ عند الاستعمال — «شيء ما غير مضبوط».
 */

/**
 * المنحنى الأساسي — خروج سريع ثم استقرار طويل (ease-out expo).
 * مأخوذ من المواصفة: cubic-bezier(0.16, 1, 0.3, 1).
 *
 * يناسب الدخول والكشف: العنصر يقطع معظم المسافة في أول ثلث الزمن فيُقرأ
 * فورياً، ثم يستقر بلا ارتداد. عكسه (ease-in) يجعل الواجهة تبدو متثاقلة.
 */
export const EASE_OUT_EXPO = 'cubic-bezier(0.16, 1, 0.3, 1)';

/** ارتداد خفيف — للأزرار المغناطيسية وحدها، لا للكشف */
export const EASE_SPRING = 'cubic-bezier(0.34, 1.56, 0.64, 1)';

/**
 * المدد.
 *
 * الخروج أسرع من الدخول دائماً: ما يغادر لا ينتظره أحد، وإبطاؤه يجعل
 * الواجهة تبدو لزجة. والنسبة نحو الثلثين.
 */
export const DURATION = {
  /** استجابة فورية — تحويم، تغيّر لون */
  instant: 150,
  /** انتقال قياسي — إظهار، تبديل حالة */
  base: 300,
  /** كشف عند التمرير */
  reveal: 620,
  /** خروج — أسرع من نظيره الداخل */
  exit: 200,
} as const;

/** فاصل التتابع بين عناصر المجموعة الواحدة */
export const STAGGER_MS = 70;

/**
 * هل يطلب المستخدم تقليل الحركة؟
 *
 * يُقرأ وقت التشغيل لا مرة واحدة: النظام يسمح بتغيير التفضيل أثناء الجلسة،
 * وقراءته عند التحميل وحده تُبقي الحركة تعمل لمن أطفأها للتوّ.
 */
export function prefersReducedMotion(): boolean {
  if (typeof window === 'undefined' || !window.matchMedia) return false;
  return window.matchMedia('(prefers-reduced-motion: reduce)').matches;
}
