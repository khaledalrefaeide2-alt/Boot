node_modules/
.next/
out/
build/
dist/
# الأسرار: كل ملفات البيئة مستثناة، وتُستثنى من الاستثناء القوالبُ وحدها.
# القاعدة عامة عمداً: قائمة بأسماء بعينها (.env، .env.local) تترك
# .env.production خارج الحماية، وهو أخطرها — يحمل كلمة مرور القاعدة
# وسرّ الجلسات ورمز Apify للخادم الحيّ.
.env
.env.*
!.env.example
!.env.production.example
src/generated/
*.tsbuildinfo
next-env.d.ts
.DS_Store
exports/

# لقطة Redis إن شُغّل خارج Docker في مجلد المشروع
dump.rdb

# ملفات يولّدها Next تلقائياً للوكلاء
AGENTS.md
CLAUDE.md
