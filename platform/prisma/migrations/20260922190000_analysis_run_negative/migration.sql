-- عدّاد المنشورات السلبية في جولة التحليل.
-- منه يُحسب إنذار «ارتفاع السلبية» بعد الجولة، بعد أن كان يُحسب في
-- الاستيراد من محرّك كلمات مفتاحية يقيس نبرة النصّ لا الموقف.
ALTER TABLE "analysis_runs" ADD COLUMN "negative" INTEGER NOT NULL DEFAULT 0;
